# 1·2·3·8·9·11·12주차 7차시 압축 운영 — 디버깅 기록

작성 2026-09-26. 전시회 2026-11-20.

0·4·5·6·7·10주차를 빼고 **1 → 2 → 3 → 8 → 9 → 11 → 12** 로 진행할 때,
학생이 바이브 코딩으로 작품을 만들다 막히는 지점을 찾아 고친 기록입니다.

---

## 요약 — 고친 것 7건

| # | 어디 | 무엇이 문제였나 | 위험도 |
|---|---|---|---|
| 1 | `week11-vibe-coding/bundle-source.js` | `SKIP_DIRS` 에 `"lib"` 이 있어 **2주차에서 만든 `src/lib/db.ts` 가 AI에게 안 올라감** | 높음 |
| 2 | 11주차 교재 | **전압·배선 안전이 10주차에만 있었음.** 10주차를 빼면 5V 경고가 사라짐 | **매우 높음 — 보드 파손** |
| 3 | `프로젝트-규칙.md` | 전압/분압/5V 언급 **0회**. AI가 5V 센서를 그냥 GPIO에 붙이라고 답함 | **매우 높음** |
| 4 | 8주차 교재 | Tailwind 제거가 *"7주차를 하고 왔다면"* 조건부 → **건너뛰면 레이아웃 깨짐** | 높음 |
| 5 | 12주차 교재 ⑥ | `{!kiosk && ...}` — `kiosk` 는 11주차 **선택** 과제 산물 | 높음 |
| 6 | 12주차 교재 ⑤ | `retrying` 이름이 팀마다 다를 수 있음 | 중간 |
| 7 | 8·11·12주차 | 없어진 주차 참조 다수 (4~7주차, 10주차, 13주차 예고) | 낮음 |

---

## 1. 소스 묶기에서 `src/lib/db.ts` 가 빠지던 문제

`bundle-source.js` 의 건너뛸 폴더 목록에 `"lib"` 이 들어 있었습니다.
의도는 `CIRCUITPY/lib`(라이브러리 폴더)였지만, 이름만 보고 지우므로
**2주차에서 만든 `src/lib/db.ts` 까지 같이 빠졌습니다.**

2주차 교재는 `src/lib/db.ts` 를 만들라고 명시하고 있으므로, 이 파일이 빠지면
제미나이는 DB 코드를 **추측해서** 새로 씁니다.

```js
// 고친 뒤
// ※ "lib" 을 넣으면 안 됩니다. 2주차에서 만든 src/lib/db.ts 가 빠져 버립니다.
const SKIP_DIRS = new Set([
  "node_modules", ".next", ".git", ".vscode", "out", "build",
]);
```

`CIRCUITPY/lib` 는 애초에 걱정할 필요가 없습니다 — Pico 쪽은 드라이브를 넘겨받아
`code.py` 만 따로 읽기 때문입니다.

**확인**: 고치기 전 6개 파일(`src/lib/db.ts` 없음) → 고친 뒤 7개(`src/lib/db.ts` 포함).
`.env.local` 과 `pico/settings.toml` 은 여전히 제외되고,
코드에 박힌 비밀번호 경고도 그대로 뜹니다.

---

## 2·3. 전압·배선 안전 — 가장 위험했던 구멍

10주차(센서 이식)가 **유일하게** 전압 경고를 갖고 있었습니다. 그 주차를 빼면
학생은 11주차에서 바로 자기 센서를 붙이는데, 5V 경고를 한 번도 못 봅니다.

특히 **HC-SR04 초음파**는 인기가 많고 `ECHO` 가 **5V**로 신호를 냅니다.
Pico GPIO는 3.3V까지라서 그대로 꽂으면 핀이 죽습니다.

### 11주차 교재에 새 4-1절을 넣었습니다

> **4-1. 센서를 붙이기 전 — 전압 확인이 먼저입니다**

- 센서별 전원/신호 표 (토양수분·HC-SR04·PIR·PMS7003·릴레이)
- HC-SR04 분압 회로 (1kΩ+2kΩ → 3.33V), 또는 HC-SR04**P** 대안
- 3V3(36번) 약 300mA 한계 / VBUS(40번) / **모든 GND 공통**
- 쓸 수 있는 핀 표 — GP23·24·25·29 금지, **아날로그는 GP27·GP28 두 개뿐**
- 배선 순서 5단계 (**USB 뽑고** 배선 → 눈으로 확인 → 꽂기)
- `code.py` 로 센서 하나만 단독 시험 + 시리얼 값 판독표(0 / 65535 / 오류)

### `프로젝트-규칙.md` 에 §7·§8 을 넣었습니다

이 문서는 학생이 제미나이에게 **함께 올리는** 파일입니다.
그래서 여기에 넣으면 **AI가 먼저 경고해 줍니다.**

> 센서를 추가하는 코드를 줄 때는, 코드보다 먼저 아래 세 가지를 알려 주세요.
> 1. 전원이 3.3V인가 5V인가
> 2. **센서가 Pico로 내보내는 신호가 5V인가** — 5V라면 분압이 필요하다고 **반드시 경고**
> 3. 3V3(36번)에서 받을지 VBUS(40번)에서 받을지

릴레이 active-low 와 시작 시 꺼짐 초기화도 여기에 넣었습니다.

---

## 4. 8주차 — Tailwind 제거가 조건부였던 문제

2주차에서 `create-next-app --tailwind` 로 프로젝트를 만들기 때문에,
`globals.css` 맨 위에 `@import "tailwindcss";` 가 **모든 학생에게** 있습니다.

그런데 8주차 교재는 이렇게 써 있었습니다.

> ~~⚠️ **7주차를 하고 왔다면 이 교체가 꼭 필요합니다.**~~

7주차를 하지 않은 학생은 *"나는 해당 없음"* 으로 읽고 건너뜁니다.
그러면 Bootstrap과 Tailwind가 같이 켜져 `.container` 등이 충돌합니다.

```
🚨 이 교체는 모든 팀이 반드시 해야 합니다. 건너뛰면 화면 레이아웃이 깨집니다.
   2주차에서 create-next-app --tailwind 로 프로젝트를 만들었기 때문에,
   지금 globals.css 맨 위에 @import "tailwindcss"; 가 들어 있습니다.
```

3주차 트러블슈팅의 *"연결/적용 버튼이 안 보인다"* 도 같은 원인(Tailwind preflight)이라,
이번 교체로 그 문제까지 사라진다는 설명을 덧붙였습니다.

`npm run dev:lan` 은 **2주차**에서 정의되므로 압축 운영에서도 문제없습니다.

---

## 5. 12주차 ⑥ — `kiosk` 를 전제하던 문제 (실측으로 확인)

12주차는 전체 완성본을 주지 않고 **★ 여섯 덩어리**만 옮기게 합니다.
그중 ⑥(전환 막대)이 이렇게 시작했습니다.

```tsx
{!kiosk && (            // ← kiosk 는 11주차 프롬프트 5 의 산물
  <div className="style-switcher p-3 mb-4">
```

그런데 11주차 **프롬프트 5(전시 모드)는 "시간이 남으면" 하는 선택 과제**입니다.
건너뛴 팀은 `kiosk` 가 없습니다.

### 실제로 돌려서 확인했습니다

11주차 모범답안에서 `kiosk` 를 전부 걷어내 **프롬프트 5를 건너뛴 팀 파일**(403줄,
`kiosk` 0회 / `retrying` 3회)을 만들고, 교재의 여섯 덩어리를 그대로 옮겨 붙였습니다.

| 변종 | 결과 |
|---|---|
| **원래 교재 ⑥** (`{!kiosk && ...}`) | `src/app/dashboard/page.tsx(299,9): error TS2304: Cannot find name 'kiosk'.` |
| **고친 교재 ⑥** (kiosk 없음) | 타입 오류 **0건**, `next build` **성공** |

교재 ⑥에서 `kiosk` 의존을 걷어냈고, 전시 모드가 이미 있는 팀은 원하면
그 덩어리를 `{!kiosk && ( ... )}` 로 감싸라고 안내했습니다.
8번 항목의 *"전환 막대 지우기"* 설명도 새 코드에 맞게 고쳤습니다.

`week12-design-styles/page_week12.tsx` 는 **11주차 모범답안 기준 참고 파일**이라
`kiosk` 를 그대로 두었고, README에 그 차이를 적어 두었습니다.

---

## 6. 12주차 ⑤ — `retrying` 이름 문제

⑤ 안에 `{connected ? "연결됨" : retrying ? "재연결 중" : "연결 안 됨"}` 이 있습니다.
`retrying` 은 11주차 프롬프트 4 의 산물인데, 제미나이가 `isReconnecting` 처럼
다른 이름으로 만들어 줄 수 있습니다. 대안 두 가지를 교재에 적었습니다.

- 팀 파일에서 재연결 상태의 이름을 찾아 `retrying` 자리에 넣기
- 그런 상태가 없으면 그 한 줄을 `{connected ? "연결됨" : "연결 안 됨"}` 으로 줄이기

다만 **프롬프트 4 자체는 전시회 필수**라고 못 박았습니다.

---

## 7. 없어진 주차 참조 정리

| 교재 | 고친 것 |
|---|---|
| 8주차 | "4~7주차와 이번 주는 방식이 다릅니다" → 4~7주차 전제 없이 다시 씀 |
| 8주차 | "5주차에 이미 설치했다면" → "이번 주에 처음 설치합니다" |
| 8주차 | "7주차에 만든 `src/components/ui/`" → Tailwind 제거 확인 안내로 교체 |
| 8주차 | 4주(8~11주차) 로드맵 → **8·9·11·12주차** 로 |
| 8주차 | 0단계 "4~7주차에 각자 고쳐 놓았을 테니" → "3주차에서 완성한 화면 그대로" |
| 11주차 | "8~10주차는 제가 드린 코드" → 8~9주차 |
| 11주차 | "10주차 이식 절차를 프롬프트로 나눈 것" → 규칙 문서 §8 기준으로 |
| 11주차 | 전시 당일 점검표의 "(10주차에 찍어둔 것)" → "(배선하면서 찍어둔 것)" |
| 11주차 | 전체 일정 표: 10주차 행 삭제, 12주차 행 추가 |
| 12주차 | "4주차에 색상 토큰을 빼둔 것과 같은 이치" → 자체 설명으로 |
| 12주차 | "다섯 덩어리" → **여섯 덩어리(①~⑥)** ※ 실제로 여섯 개였음 |
| 12주차 | "다음 주 예고 13주차" → "이 다음에" / "남은 수업" |

---

## 11주차 시간 배분 — 15분이 늘었습니다

10주차가 없어져 11주차가 이식 실습까지 맡게 되었습니다.

| 구간 | 전 | 후 |
|---|---|---|
| 도입 | 15분 | **10분** |
| 준비 (소스 묶기·비밀번호 확인) | 15분 | 15분 |
| **안전 (4-1절, 신설)** | — | **15분** |
| 실습 1 (프롬프트 0 계획) | 15분 | **10분** |
| 실습 2 (프롬프트 1~3) | 35분 | 35분 |
| 실습 3 (프롬프트 4 자동 재연결) | 25분 | 25분 |
| 마무리 | 15분 | **10분** |
| 합계 | 120분 | **120분** |

> 4-1절은 **팀마다 센서가 다르므로 개별 지도가 필요한 구간**입니다.
> 2시간 안에 모든 팀의 이식을 끝내기는 어렵습니다.
> 교재에는 4-1절 4번(`code.py` 단독 시험)까지를 수업에서 하고,
> 나머지 이식은 방과후에 프롬프트 1~2를 반복하도록 적었습니다.

---

## 아직 남은 것

- 워크시트 docx: 1주차 CdS 배선 설명이 배선도와 어긋남 (가장 급함)
- 5~12주차 학생 워크시트 미작성
- `vibe_coding_guide.docx` 가 아직 "4~7주차에서 배운 네 가지" / "개별 프로젝트" 로 되어 있음
- 스크린샷 노션 업로드: `api.notion.com` 이 조직 프록시에서 403으로 막혀 있어
  **수동 드래그·드롭 필요** (저장소 `worksheets/screenshots/` 에 전부 있음)
