# 11주차 · 바이브 코딩으로 우리 팀 화면 완성하기

노션 교재 **「11주차 · 바이브 코딩으로 우리 팀 화면 완성하기」** 의 배포 파일입니다.

학생들은 18세 미만이라 AI 에이전트형 도구(Claude Code, Codex, Antigravity 등)에 제약이 있어,
**학교 구글 계정의 제미나이 채팅**에 소스와 규칙 문서를 올리고 프롬프트로 요청하는 방식을 씁니다.

## 학생에게 나눠줄 파일

| 파일 | 용도 |
|---|---|
| `bundle-source.js` | **프로젝트 폴더에 넣고 실행** — 소스를 한 파일로 묶음 |
| `프로젝트-규칙.md` | **소스와 함께 제미나이에 업로드** — 컨텍스트 문서 |
| `프롬프트-모음.md` | 프롬프트 8개 + 붙여넣기 전 검수 체크리스트 |

## 선생님용

| 파일 | 용도 |
|---|---|
| `page_week11_reference.tsx` | 자동 재연결 + 전시 모드 **모범답안** — 학생 결과와 대조 |

## 소스 묶기 사용법

```bash
node bundle-source.js              # 화면 코드만
node bundle-source.js D:\          # Pico(CIRCUITPY) 코드도 같이
```

`우리팀-소스.md` 가 만들어지고, **무엇이 들어갔고 무엇이 빠졌는지 화면에 출력**됩니다.

### 자동으로 빠지는 것

| 대상 | 이유 |
|---|---|
| `.env*` | DB 접속 정보 |
| `settings.toml` | **CIRCUITPY_WIFI_PASSWORD — 실습실 공유기 비밀번호** |
| `node_modules/`, `.next/`, `.git/` | 용량 (업로드 100MB 제한) |
| 이미지·바이너리 | 불필요 |

추가로, **코드 안에 직접 적어둔 비밀번호도 찾아서 경고**합니다.
(`const WIFI_PASSWORD = "..."` 같은 줄)

## 검증 결과 (2026-09-19)

### 소스 묶기 스크립트

비밀번호가 든 가짜 프로젝트를 만들어 실제로 돌렸습니다.

| 확인 | 결과 |
|---|---|
| `.env.local` 제외 | ✅ |
| `pico/settings.toml`, `CIRCUITPY/settings.toml` 제외 | ✅ |
| `node_modules/`, `.next/` 건너뜀 | ✅ |
| 스크립트 자기 자신 제외 · 같은 내용 중복 제거 | ✅ |
| **코드에 박힌 비밀번호 경고** (`src/app/layout.tsx:21`) | ✅ |
| 출력 크기 | 18.3 KB (업로드에 충분히 작음) |

### 자동 재연결 모범답안

모의 Pico를 실제로 껐다 켜며 확인했습니다.

| 상황 | 기대 | 결과 |
|---|---|---|
| 연결 직후 | ONLINE | ✅ ONLINE |
| **Pico를 끈 뒤** | 재연결 중… | ✅ 재연결 중… |
| **아무것도 안 누르고 다시 켠 뒤** | 저절로 ONLINE | ✅ ONLINE |
| **사람이 "연결 끊기"를 누른 뒤** | OFFLINE 유지 | ✅ OFFLINE (5초 뒤에도) |

타입 오류 0건 / 프로덕션 빌드 성공.

## 수업에서 강조할 것

1. **비밀번호가 안 올라간 것을 눈으로 확인**한 뒤에 업로드
2. **"아직 코드를 고치지 마세요"** — 계획부터 받기
3. **한 번에 하나씩** — 10주차 이식 절차와 같음
4. **붙여넣기 전 검수** — 특히 `machine`(MicroPython)과 Tailwind 클래스
5. **고치기 전 `.bak` 복사**
